<% Object.entries(properties).forEach(([propKey, propVal]) => { -%>
	<% gormType = helpers.filterGormType(propVal) -%>
	<% if (gormType) {gormType = `type:${gormType}`} -%>
	<%= propVal.pascalName %>      <%= propVal.goType %>       `json:"<%= propKey %>" gorm:"<%= gormType %>"`
<% }) -%>
	<%= newPropsPlaceholder %>
