<% modelScheme && modelScheme.props.forEach((prop) => { -%>
	if err := tx.Table("<%= tableName %>").Migrator().DropColumn(&<%= className %>{}, "<%= prop.propKey %>"); err != nil {
		return err
	}
<% }) -%>
