<% if (modelScheme && modelScheme.props) { -%>
	<% propArr = Object.values(modelScheme.props) -%>
	<% propArr = helpers.importPackage(propArr) -%>
	<% propArr.forEach((package) => { -%>
		"<%=package%>"
	<% }) -%>
	<% if (!isCustomChoice && !propArr.includes('time') && type !='newProp') { -%>
		"time.Time"
	<% } -%>
<% } -%>

