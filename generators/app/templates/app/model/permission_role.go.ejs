package model

import (
	"github.com/rayyone/go-core/helpers/str"
	"gorm.io/gorm"
)

type PermissionRole struct {
	PermissionRoleTbl
}

type PermissionRoleTbl struct {
	ID           string `json:"id"`
	PermissionID string `json:"permission_id"`
	RoleID       string `json:"role_id"`
	// {{ry_placeholder:new_props}}
	Timestamps
}

// TableName sets the insert table name for this struct type
func (model *PermissionRole) TableName() string {
	return "permission_roles"
}

func (model *PermissionRole) GetAccessors() {}

func (modelTbl *PermissionRoleTbl) BeforeCreate(scope *gorm.DB) (err error) {
	if modelTbl.ID == "" {
		modelTbl.ID = str.UUID()
	}
	return
}
